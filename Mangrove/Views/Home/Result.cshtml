@model Mangrove.Data.TblMangrove

@{
	ViewData["Title"] = Model.Name;
	Layout = Helper.Path.layoutUser;
	string _com = Helper.Path._components;

	TempData["View"] = Model.View;
}

@await Html.PartialAsync(_com + "/Back.cshtml")
@await Html.PartialAsync(_com + "/Infomation_Tree.cshtml")
@await Html.PartialAsync(_com + "/Back.cshtml")
